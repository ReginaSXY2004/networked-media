<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Shrine Fortunes — Sign In</title>
    <link rel="stylesheet" href="/css/auth.css">
</head>
<body>
    <div class="container">
        <div class="auth-card">
            <h2>Log in to your account</h2>
            <p class="subtitle">Welcome back, seeker of fate.</p>
            
            <% if (typeof error !== 'undefined' && error) { %>
                <div class="error-message">
                    <% if (error === 'userNotFound') { %>
                        userNotFound
                    <% } else if (error === 'wrongPassword') { %>
                        wrongPassword
                    <% } else { %>
                        <%= error %>
                    <% } %>
                </div>
            <% } %>
            
            <form method="POST" action="/authenticate">
                <div class="form-group">
                    <input type="text" name="username" placeholder="Your name" required>
                </div>
                <div class="form-group">
                    <input type="password" name="password" placeholder="Password" required>
                </div>
                <div class="form-group">
                    <button type="submit" class="btn-submit">I'm ready</button>
                </div>
            </form>
            
            <p class="switch-auth">Don't have an account? <a href="/register">Register here</a></p>
        </div>
    </div>
</body>
</html>